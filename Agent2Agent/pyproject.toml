[project]
name = "agent2agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Varun2480", email = "varuntej2480@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "a2a-samples",
]

[project.scripts]
agent2agent = "agent2agent:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
a2a-samples = { git = "https://github.com/google/A2A", subdirectory = "samples/python" }
